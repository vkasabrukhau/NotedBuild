import { Quicksand, Silkscreen } from "next/font/google";

export const quicksand = Quicksand({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const silkscreen = Silkscreen({
  subsets: ["latin"],
  weight: ["400"],
});
